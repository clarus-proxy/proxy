description = 'Searchable encryption (SE) module for the CLARUS proxy'

dependencies {
    // CLARUS
    compile project(':proxy-common')
    compile group: 'eu.clarussecure', name: 'dataoperations.SE_module', version: '1'
}
