description = 'Encryption module for the CLARUS proxy'

dependencies {
    // CLARUS
    compile project(':proxy-common')
    compile group: 'eu.clarussecure', name: 'dataoperations.encryption', version: '0.1-SNAPSHOT'
}
